# RepoRanger Workflow Template (Version 1.0)
name: RepoRanger
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
    inputs:
      task:
        description: 'Task to run (review/janitor)'
        default: 'review'
      target_number:
        description: 'PR or Issue number to act on'
        required: false
      dead_branch_threshold:
        description: 'Days before a branch is considered stale'
        required: false
        default: '10'
      branch_name:
        description: 'Branch name for protect_branch task'
        required: false
        default: ''

jobs:
  reporanger:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      issues: write
      contents: write
    steps:
      - name: Checkout Monorepo
        uses: actions/checkout@v3
        with:
          repository: 'shrujaya/repo-ranger' # Points back to the central RepoRanger repo
          ref: 'main'
          path: './reporanger'

      - name: Run RepoRanger Worker
        uses: ./reporanger/worker
        with:
          task: ${{ github.event.inputs.task || (github.event_name == 'schedule' && 'scheduled_janitor' || (github.event_name == 'issues' && 'janitor' || 'review')) }}
          target_number: ${{ github.event.issue.number || github.event.pull_request.number || github.event.inputs.target_number }}
          dead_branch_threshold: ${{ github.event.inputs.dead_branch_threshold || '10' }}
          branch_name: ${{ github.event.inputs.branch_name || '' }}
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          DISPATCHER_URL: 'https://repo-ranger-dispatcher.vercel.app' # Replace with actual Vercel URL
